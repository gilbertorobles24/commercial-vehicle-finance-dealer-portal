spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dealerdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update  # dev only
    show-sql: true
server:
  port: 8080